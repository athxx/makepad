[package]
name = "makepad-mlx"
version = "0.1.0"
edition = "2024"
description = "Rust MLX runtime port for Gemma 4 on Apple Silicon"
license = "MIT"

[dependencies]
makepad-ggml = { path = "../ggml" }
makepad-llama = { path = "../llama" }
makepad-micro-serde = { path = "../micro_serde" }
makepad-zune-core = { path = "../../vendor/zune/zune-core" }
makepad-zune-jpeg = { path = "../../vendor/zune/zune-jpeg" }
makepad-zune-png = { path = "../../vendor/zune/zune-png" }
