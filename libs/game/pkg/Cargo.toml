[package]
name = "makepad-game-pkg"
version = "0.1.0"
edition = "2024"
description = "Game packaging, hardened extraction and registry client for Makepad Arcade (see game.md)"
license = "LGPL-3.0"

[dependencies]
makepad-zip-file = { path = "../../zip_file", version = "1.0.0" }
