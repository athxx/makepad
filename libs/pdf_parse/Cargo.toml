[package]
name = "makepad-pdf-parse"
version = "1.0.0"
edition = "2024"
description = "Minimal PDF parser for Makepad - text, vector graphics, and images"
license = "LGPL-3.0"

[dependencies]
makepad-zune-inflate = { path = "../../vendor/zune/zune-inflate", version = "0.2.0", default-features = false, features = ["zlib"] }
